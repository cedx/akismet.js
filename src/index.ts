const {Author} = require('./author.js');
const {Blog} = require('./blog.js');
const {Client, ClientError} = require('./client.js');
const {Comment, CommentType} = require('./comment.js');

module.exports = {
  Author,
  Blog,
  Client,
  ClientError,
  Comment,
  CommentType
};
