{
	"bugs": "https://github.com/cedx/akismet.js/issues",
	"description": "Prevent comment spam using the Akismet service.",
	"homepage": "https://github.com/cedx/akismet.js",
	"license": "MIT",
	"name": "@cedx/akismet",
	"repository": "cedx/akismet.js",
	"type": "module",
	"version": "16.0.2",
	"author": {
		"email": "cedric@belin.io",
		"name": "Cédric Belin",
		"url": "https://belin.io"
	},
	"devDependencies": {
		"@types/node": "^20.11.5",
		"eslint": "^8.56.0",
		"typedoc": "^0.25.7",
		"typescript": "^5.3.3"
	},
	"engines": {
		"node": ">=20.0.0"
	},
	"exports": {
		"types": "./lib/index.d.ts",
		"import": "./lib/index.js"
	},
	"files": [
		"lib/",
		"src/"
	],
	"imports": {
		"#akismet": {
			"types": "./lib/index.d.ts",
			"import": "./lib/index.js"
		}
	},
	"keywords": [
		"akismet",
		"api",
		"client",
		"comment",
		"spam",
		"validation"
	],
	"scripts": {
		"build": "node scripts/build.js && tsc --project src/tsconfig.json",
		"clean": "node scripts/clean.js",
		"doc": "typedoc --options etc/typedoc.json && node scripts/doc.js",
		"dist": "npm run clean && npm run build",
		"lint": "tsc --project tsconfig.json && eslint --config=etc/eslint.json example scripts src test",
		"postpublish": "node scripts/publish.js",
		"prepack": "npm run dist",
		"test": "npm run build && node --test --test-reporter=lcov --test-reporter-destination=var/lcov.info --test-reporter=spec --test-reporter-destination=stdout"
	}
}
